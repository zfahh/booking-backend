export enum AuditAction {
  CONCERT_CREATED = 'concert_created',
  CONCERT_UPDATED = 'concert_updated',
  CONCERT_DELETED = 'concert_deleted',
  BOOKING_CREATED = 'booking_created',
  BOOKING_CANCELLED = 'booking_cancelled',
}
